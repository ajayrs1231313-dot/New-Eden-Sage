{
  "name": "new-eden-sage-online-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply new-eden-sage-online --local",
    "db:migrate:remote": "wrangler d1 migrations apply new-eden-sage-online --remote"
  },
  "devDependencies": {
    "typescript": "latest",
    "wrangler": "^4.124.0"
  },
  "dependencies": {
    "jose": "^6.2.9"
  }
}
